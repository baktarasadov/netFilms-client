"use client";
import { IModal } from "@/types/ui/IModal";
import { memo } from "react";

const Modal: React.FC<IModal> = ({ handleClick, title, openModal }) => {
  console.log("Rebalancing");

  return <>Ok</>;
};

export default memo(Modal);
